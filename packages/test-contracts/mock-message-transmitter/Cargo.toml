[package]
name = "mock-message-transmitter"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]
doctest = false

[dependencies]
cctp-interfaces = { workspace = true }
cctp-utils = { workspace = true }
soroban-sdk = { workspace = true }
stellar-utils = { workspace = true, features = ["hazmat-address-utils"] }
